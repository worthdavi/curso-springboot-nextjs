import { Layout } from "components/layout"

export const CadastroProdutos: React.FC = () => {
    return (
        <Layout titulo="Cadastro de Produtos">
            Este é o children da tag
        </Layout>
    )
}